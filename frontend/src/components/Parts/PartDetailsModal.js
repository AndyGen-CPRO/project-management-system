import React, { useState } from 'react';
import axios from 'axios';

const PartDetails = ({ closeModal, parts, fechParts, token }) => {
    const [name, setName] = useState("");
    const [description, setDescription] = useState("");

    //part detail + parts under it
    return (
        <div>
            
        </div>
    )
}